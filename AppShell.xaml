<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="TurnupFrontendDotNet6.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:TurnupFrontendDotNet6"

    Shell.FlyoutBehavior="Flyout">
     

    <!-- <ShellContent -->
    <!--     Title="Main" -->
    <!--     ContentTemplate="{DataTemplate local:MainPage}" -->
    <!--     Route="MainPage" /> -->
    
     <ShellContent
         Title="Register"
        ContentTemplate="{DataTemplate local:RegistrationPage}"
        Route="RegistrationPage" />
    
     <ShellContent
         Title="Login"
        ContentTemplate="{DataTemplate local:LoginPage}"
        Route="LoginPage" />
    
    <ShellContent
        
        Title="Establishment"
        ContentTemplate="{DataTemplate local:LoadingPage}"
        Route="LoadingPage" />
    
    <!--  <ShellContent -->
    <!--     Title="Scan" -->
    <!--     ContentTemplate="{DataTemplate local:ScanPage}" -->
    <!--     Route="ScanPage" /> -->

</Shell>

